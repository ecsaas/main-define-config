# main-define-config

```shell
 git init
 git branch -m main

 git config user.name manhbv.giaminh
 git config user.email manhbv.giaminh@gmail.com
 git add .
 git commit -m 'main-define-config'

 git remote add origin git@github.com:ecsaas/main-define-config.git
 git push origin main
```
